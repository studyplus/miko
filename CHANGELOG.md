# Changelog

## v1.5.0 (2026-09-05)

### New

- **横断カテゴリ（cross-cutting category）を導入** — 複数ケイパビリティに効果が及ぶルール（例: 規約違反ユーザーの発信制限）を管理する仕組み。BR の階層（ケイパビリティ / カテゴリ / ルール）を（パッケージ / モジュール / 関数）と見立て、横断カテゴリを**カテゴリの可視性修飾**として表現する: オーナーは概念のカテゴリ見出しに `[横断]` マークと `<!-- cross-category -->` マーカーを付けて public 化し（lead 文が宣言文、定義はカテゴリ内の通常ルール）、効果を持つケイパビリティは自分のルールに `[xc:{cap}/{プレフィックス}]` タグを付けて参照する（import に相当）。非適用は記録しない。一覧は `grep 'cross-category' miko/*/business_rules.md`、特定概念の全体像（宣言 + 全適用ルール）は ID の grep で都度導出し、中央レジストリは持たない。public カテゴリの改名・分割・マーク変更は proposal 必須（private カテゴリの再編は従来どおり自由）。網羅性は記録や事後監査ではなく **プロセスの門で構成的に保証する** — ケイパビリティ × 概念の全ペアは、概念の宣言（propose の影響調査）かケイパビリティの新規作成（new-cap の判定ステップ）の遅い方で必ず一度検討される。判定テスト（他者性・全称性・必達性）、ID・マーク・タグの書式、書き込み時 lint（orphan 参照・マーク整合・書式）を `business_rules_guide.md` の「横断カテゴリとは何か」に定義。`.miko/examples/business_rules.md` に実例（横断カテゴリ `xc:order_management/DEFAULT` とタグ付きルール ORD-05）を追加。検討経緯と却下した代替案（中央レジストリ・ルール単位マーク・適合セクション・非適用の記録・グローバル連番 ID・専用セクション + 宣言行・harae での網羅監査）は `business_rules_driven_development.md` の「解決済みの論点」を参照

### Changed

- **`/miko.propose` の他ケイパビリティ影響調査を任意から必須に変更** — 従来の「調査しますか？」の確認をやめ、デフォルトで実行する（主さまが明示的にスキップを指示した場合のみ省略し、proposal に記録）。横断カテゴリの網羅保証の門その 1。調査ガイドに横断カテゴリの観点（宣言・定義に触れる変更の影響先を ID の grep で特定、昇格候補の報告）を追加
- **`/miko.new-cap` に全ケイパビリティ BR の関連調査（サブエージェント・必須）と横断カテゴリの判定ステップを追加** — 従来は「外部との接点」に挙がった隣接 BR だけを読んでいたが、新規ケイパビリティ作成時が横断ルールの取りこぼしが最も起きやすいため、全 BR の走査と、宣言済み横断カテゴリの判定（grep → 対話 → 効果を持つならタグ付きルールを作成）を必須化。横断カテゴリの網羅保証の門その 2
- **BR を更新するスキルに書き込み時 lint を追加** — `/miko.quick-impl`・`/miko.speckit.implement`・`/miko.catchup`・`/miko.quick-catchup`・`/miko.new-cap` は BR 更新直後に横断カテゴリの構文検査（orphan `xc:` 参照・`[横断]` マークとマーカーの整合・横断カテゴリとタグの書式）を実行する

## v1.4.2 (2026-07-28)

### Changed

- **glossary.md のケイパビリティ節を「配置の軸」と定義** — 節がスコープに見えることが「ケイパビリティごとに意味が変わってよい」という誤解の元だった。節が示すのは「どのケイパビリティが定義に責任を持つか」であり、「どの範囲でこの意味が通じるか」ではない。用語は本質的にケイパビリティに閉じないが、整理の単位としてはケイパビリティが扱いやすいので節は維持する。定義はシステム全体で通用し、他のケイパビリティの BR から参照してよい。同じ意味の用語を節ごとに重複させず、どのケイパビリティにも帰属しない語だけを「全体」に置く
- **glossary.md の見出し語をシステム全体で一意にする方針に変更** — 従来は「ケイパビリティごとに意味が異なる場合はスコープ付きで併記する」としていたが、BR 本文に現れる語自体はスコープを持たないため、読み手が対象ケイパビリティを知らないと意味が引けず、ケイパビリティ境界や横断影響の検討で前提が崩れていた。同じ語が別概念を指す場合は修飾を付けた一意な別名を立てて別項目にし（注文確定 / 決済確定）、BR 本文でも別名の側を使う。現場での呼び方は定義文の末尾に残して逆引きできるようにする。`business_rules_guide.md`（ドキュメント体系・語彙規律）、`.miko/examples/glossary.md`、`tone_guide.md` / `tone_guide.en.md`（略称からの別名特定）、`harae_guide.md`（用語衝突の検査）、`/miko.setup`・`/miko.new-cap`・`/miko.catchup`・`/miko.quick-catchup`・`/miko.propose` の glossary 更新手順に反映

## v1.4.1 (2026-07-23)

### Fixed

- **glossary.md に実装の詳細が書かれてしまう問題を修正** — 用語集の項目にクラス名・メソッド名・API・処理手順まで書かれ、実装変更のたびに追従が必要になっていた。`.miko/examples/glossary.md` に「実装技術を置き換えても定義文は変わらないか」という判定テストと悪い例/良い例を追記し、`/miko.setup`・`/miko.new-cap`・`/miko.catchup`・`/miko.quick-catchup`・`/miko.propose` の glossary.md 更新手順にも「実装の詳細は書かず1〜2文の定義だけを書く」旨を明記

## v1.4.0 (2026-07-17)

### New

- **スキル名の区切り文字（ハイフン / ドット）を選択可能に** — 一部の LLM プラットフォームは Agent Skills 準拠のハイフン区切りのスキル名しか使えないため、インストール時にどこでも動作するハイフン区切り（`/miko-setup`、デフォルト）とドット区切り（`/miko.setup`）を選択できるようにした。設定は `.miko/config` の `separator=` に保存され、スキルディレクトリ名とファイル内のスキル名参照（handoffs 含む）がインストール・アップグレード時に自動変換される。後から変更する場合は miko を削除して再インストールする。tone_guide に「出力でスキル名に言及するときは config の separator に従う」を明記

### Changed

- **スキル名のアンダースコアをハイフンに統一** — `miko.new_cap` → `miko.new-cap` のように、スキル名内の `_` を `-` に変更（対象: new-cap / new-harae / quick-catchup / quick-impl / split-proposal / catchup-system-hld）。ハイフン区切り選択時に `miko-new_harae` のような混在表記になるのを避けるため。既存インストールはアップグレード時に旧名スキルが削除され新名で再配置される

### Fixed

- **upgrade.sh が macOS 標準の bash 3.2 で中断する問題を修正** — `.miko/protected_skills` にプロテクト対象がない場合、`set -u` 下での空配列展開により処理が中断していた

## v1.3.2 (2026-06-26)

### Changed

- **空疎・気取り表現の回避ガイドラインを追加** — LLM が生成しがちな「予告・総括」「姿勢の宣言」「空虚な形容・動詞」「無内容な接続」などを `tone_guide.md` / `tone_guide.en.md` に明記。演出の抑制（溜め・太字多用・一意でない比喩の禁止）と、巫女口調との関係（語尾・丁寧さは維持、修辞的空虚のみ禁止）も合わせて定義

## v1.3.1 (2026-06-23)

### Changed

- **既存 BR の構造を保持する方針を明文化** — v1.3.0 で導入したビジネスポリシー層・2.1/2.2 分割は新規ケイパビリティ向けであり、既存 `business_rules.md` には遡及適用しない旨を `business_rules_guide.md` と `skills/miko.propose/SKILL.md` に追記。既存 BR の構造もルール本文も、ユーザーから明示的な移行依頼がない限り変更しない（文字数や語彙規律など現在基準への合わせ込みも含めて）

## v1.3.0 (2026-06-23)

### New

- **ビジネスポリシー（ビジネス方針）層を導入** — ケイパビリティに通底する価値判断・指針を `## ビジネスポリシー（ビジネス方針）` セクション（`POL-{連番}`）として business_rules.md に新設。判定は「客観性」テスト（任意のケースで客観的に判定できるか）— Yes ならルール、No ならポリシー。コードから抽出する際にルールに紛れ込みがちな価値判断を意識的に分離する層。ルールとポリシーは整合関係であり、機械的な導出関係ではない（ルール → ポリシーのバックリンクは持たない）
- **ビジネスルールをドメイン / システムに分割** — ビジネスルール・カタログを `### 2.1 ドメインビジネスルール`（紙の運用でも成り立つもの）と `### 2.2 システムビジネスルール`（非同期・冪等性・整合性回復など、システム前提のもの）の 2 セクションに分けて配置。判定は「紙運用」テスト。1 ルール内にドメイン判断とシステム判断が混在することを避ける

### Changed

- `business_rules_guide.md`・`business_rules_driven_development.md`・`examples/business_rules.md`・`tone_guide.en.md` を上記 2 点に追従して更新

## v1.2.0 (2026-06-18)

### Changed

- **BR 1本の粒度・長さに歯止めを追加** — 「本文がおおむね150字超」または「独立した判断が3つ以上」を詰め込みすぎのサイン（黄信号）とする基準を `business_rules_guide.md`・`business_rules_driven_development.md` に明文化。文の数に固定の上限は設けない。黄信号が出たら 分解 / 経緯を本文から落とす / 仕様を機能仕様・HLD へ除外 のいずれかで対処する。150字はハード上限ではなく再点検の合図
- **禊（misogi）の事前審査に「粒度・長さ検査」を追加** — サブエージェントが BR 候補の長さ過大・判断の同居を「粒度過大」として指摘するようにした

## v1.1.1 (2026-06-14)

### Changed

- **proposal 受け付けスキルのケイパビリティ名指定を廃止** — `miko.quick_impl` / `miko.split_proposal` / `miko.speckit.specify` でケイパビリティ名だけを渡したときに最新プロポーザルを自動選択する挙動を削除。プロポーザルファイルのパス直接指定のみに統一した

## v1.1.0 (2026-06-10)

### New

- **出力言語の選択に対応（日本語 / English）** — インストール時に出力言語を選択できるようにした。選択は `.miko/config` に保存され、対話と生成ドキュメントの言語が統一される。`MIKO_LANG=en` 環境変数でプロンプトを省略可能
- **英語版 tone_guide（`tone_guide.en.md`）を追加** — 英語の対話トーンに加え、生成ドキュメントの正準見出し対訳表を含む。スキル・ガイド・examples は日本語のまま、出力のみ英語化する方式
- **英語版 README（`README.en.md`）を追加** — 両 README の冒頭に言語リンクを設置

### Changed

- tone_guide に「出力言語」セクションを追加し、適用範囲を対話のみから全出力（生成ドキュメント含む）に拡大
- speckit 系 7 スキルに tone_guide への参照を追加（従来は未参照だった）
- upgrade.sh が `.miko/config` の言語設定を読んで tone_guide を解決するようにした（config がない既存インストールは日本語扱い）
- install.sh / upgrade.sh 自体のメッセージも選択言語で表示するようにした

## v1.0.0 (2026-06-10)

- **OSS 公開に合わせて v1.0.0 とした** — 以降の変更履歴はこのファイルに記載する
